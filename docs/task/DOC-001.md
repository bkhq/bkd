# DOC-001 Fix bkd skill API examples

- status: completed
- priority: P2
- owner: doc-fix-session-20260324
- created_at: 2026-03-24 16:27 UTC
- updated_at: 2026-03-24 16:28 UTC
- scope: align `skills/bkd/SKILL.md` with the current BKD API routes and payload shapes

## Goal

Correct the verified API documentation drift in the local `bkd` skill so the examples match the current backend behavior.

## Context

- The repository contains a local skill at `skills/bkd/SKILL.md`, but it is not part of the current session's built-in skill list.
- Static route inspection and live API checks confirmed several documentation mismatches.
- The requested scope is documentation-only. No backend behavior changes are required.

## Deliverables

- Correct archive and unarchive project examples
- Correct `envVars` payload example for project updates
- Correct default engine payload example
- Correct processes response description and monitoring example
- Clarify that cron log lookup uses a job ID, not a job name

## Outcome

- Updated `skills/bkd/SKILL.md` to match the current backend routes and payload shapes for the verified drift points.
- Re-checked the edited sections against the route implementations in `apps/api/src/routes/projects.ts`, `engines.ts`, `processes.ts`, and `cron.ts`.
- Left backend code unchanged because the issue was documentation-only.
