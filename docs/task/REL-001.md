# REL-001 Upgrade launcher Bun runtime

- **status**: completed
- **priority**: P1
- **owner**: current-session
- **createdAt**: 2026-05-11 00:00

## Description

Upgrade the distributed launcher so newly downloaded launcher binaries are built with the current Bun runtime instead of the older runtime embedded in the existing `launcher-v1` release.

Acceptance criteria:

- Launcher build workflow uses the intended Bun runtime version.
- Launcher release assets are rebuilt from `main`.
- Verification records the Bun version used for the launcher build.

## ActiveForm

Upgrading the launcher Bun runtime.

## Dependencies

- **blocked by**: (none)
- **blocks**: (none)

## Notes

- Current `launcher-v1` release was published on 2026-03-11 and points to commit `35254763c5ca2412697697ba6ba6db6024411822`.
- `.github/workflows/launcher.yml` uses `oven-sh/setup-bun@v2` without a `bun-version`, so the launcher runtime is determined by the workflow execution date.
- Local Bun is `1.3.13` (`1.3.13+bf2e2cecf`), matching the latest stable release observed during investigation.
- 2026-05-11: User approved option 2: make workflow Bun selection explicit and handle runtime refreshes through workflow-based updates.
- 2026-05-11: User corrected the implementation direction: the workflow should request `latest` because Bun is the embedded launcher runtime.
- 2026-05-11: Verification passed: `bun --version`, `git diff --check`, `bun scripts/compile.ts --mode launcher --target bun-linux-x64 --outfile bkd-launcher-linux-x64-test`, and `./dist/bkd-launcher-linux-x64-test --version`.
- 2026-05-11: Release rebuild remains pending until the workflow change is committed and pushed to `main`; then run `gh workflow run launcher.yml --ref main`.
- 2026-05-11: Commit `0a2b3ab` was pushed to `main`.
- 2026-05-11: Launcher workflow run `25651691265` completed successfully and updated `launcher-v1`.
