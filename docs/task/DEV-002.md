# DEV-002 Upgrade all workspace dependencies to latest

- **status**: completed
- **priority**: P2
- **owner**: cd
- **createdAt**: 2026-07-17

## Goal

Bring every dependency across the monorepo (`@bkd/root`, `@bkd/api`,
`@bkd/frontend`, catalog) up to the latest stable version published on
npmjs.com, verified via `bun outdated` on 2026-07-17.

## Scope

- Minor/patch bumps (28 packages): hono, @hono/zod-openapi, react/react-dom,
  react-router-dom, @tanstack/*, shiki + @shikijs/*, vite, vitest, tailwindcss,
  eslint, i18next, react-i18next, dompurify, lucide-react, zustand, sonner,
  @base-ui/react, @pierre/diffs, @uiw/react-codemirror, @codemirror/lang-markdown,
  @nsio/nsl, @libsql/client, @types/*, @vitejs/plugin-react, @tailwindcss/vite,
  shadcn, eslint-plugin-react-refresh.
- Major bumps (verified changelogs, low risk):
  - nanoid 5 → 6 (perf rewrite; drops Node 18/20 — irrelevant on Bun; API unchanged)
  - @atlaskit/pragmatic-drag-and-drop 1 → 2 + hitbox 1 → 2 (only drops TS4 type
    shims; TS5+ required — we are on TS6)
  - @hono/zod-validator 0.8 → 0.9 (uses `InferInput` from hono; requires
    hono >= 4.11.2 — satisfied)
  - @antfu/eslint-config 8 → 9 + @eslint-react/eslint-plugin 3 → 5 (breaking:
    eslint-react 5.0; dev-only, lint autofix expected)
- Major bump (high risk, verify-then-decide): typescript 6.0.3 → 7.0.2
  (catalog). If `tsc -b`, eslint, or tests break, stay on ^6.x with a recorded
  PINNED reason.

## Verification

1. `bun install` clean
2. `bun run lint` passes (autofix allowed)
3. `bun run test` (api + frontend) passes
4. `bun run build` (tsc -b + vite build) passes

## Plan

See [PLAN-013](../plan/PLAN-013.md).
