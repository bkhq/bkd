# DEV-003 Replace @nsio/nsl with @dotns/nsl

- **status**: completed
- **priority**: P3
- **owner**: cd
- **createdAt**: 2026-07-17

## Goal

The nsl dev-URL proxy moved from the `@nsio` scope to `@dotns`
(github.com/dotns/nsl). Swap the root devDependency to `@dotns/nsl@^0.2.1`
(latest) and update references.

## Notes

- 0.2.0 breaking change only removes the server-side pieces (nsld + tunnel);
  this repo uses only the local `nsl run` / `nsl start` proxy — unaffected.
- Updated: root `package.json`, `AGENTS.md`, `docs/development.md`.
- Verified: `bun install` clean (old package removed), `bunx nsl --version`
  resolves to 0.2.1 from the new scope.
