# ──────────────────────────────────────────────
# Vite Dev Server
# ──────────────────────────────────────────────
# VITE_DEV_PORT=3000         # Dev server port (default: 3000)
# VITE_DEV_HOST=0.0.0.0      # Dev server host (default: 0.0.0.0)

# Note: dev URL routing is handled by nsl (http://bkd.localhost). The
# Vite dev server no longer proxies /api/* — access the app via bkd.localhost.
