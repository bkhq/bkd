/**
 * Static asset map — empty stub for development mode.
 *
 * During `bun run compile`, the compile script replaces this file with
 * generated imports that embed all frontend/dist files into the binary.
 * After compilation, the stub is restored.
 */
export const staticAssets = new Map<string, string>()
