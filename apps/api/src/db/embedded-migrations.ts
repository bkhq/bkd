/**
 * Embedded migration data — empty stub for development mode.
 *
 * During `bun run compile`, the compile script replaces this file with
 * generated content that embeds all drizzle migration files into the binary.
 * After compilation, the stub is restored.
 */
export const embeddedMigrations = new Map<string, string>()
